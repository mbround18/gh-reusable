{
  "name": "@gh-reusable/docker-facts-action",
  "version": "1.0.0",
  "private": true,
  "description": "Docker facts action implemented in TypeScript.",
  "main": "dist/index.js",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
