name: Ensure Repository
description: "Fails the workflow if it is running outside the expected repository."
author: "mbround18"

inputs:
  repository:
    description: "Expected repository in owner/repo format (e.g. mbround18/gh-reusable)."
    required: true

runs:
  using: node24
  main: dist/index.js
