{
  "name": "@gh-reusable/dagger-report-action",
  "version": "1.0.0",
  "private": true,
  "description": "Surfaces structured Dagger pipeline reports as GitHub Actions log groups and a job summary.",
  "main": "dist/index.js",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
